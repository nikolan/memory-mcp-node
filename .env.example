# API keys only - memoryDir and other settings go in config.json

# Option 1: Fully local (no keys needed)
# Just works - local embeddings, no maintenance features

# Option 2: OpenAI (local embeddings + LLM for maintenance)
OPENAI_API_KEY="sk-..."

# Option 3: Voyage embeddings (optional, for higher quality embeddings)
# VOYAGE_API_KEY="pa-..."
